{{template "base" .}}

{{define "content"}}
<!doctype html>
	<div class="container">
		<div class="row">
			<div class="col">
				<h1>this is about page</h1>
				<p>this came from the template: {{index .StringMap "test"}}</p>
			</div>
		</div>
	</div>
{{end}}